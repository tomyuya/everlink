# EverLink runtime dependencies (Python 3.14)
# Verified working versions from Phase A setup.

# --- agent framework (hard requirement: Strands Agents SDK) ---
strands-agents==1.54.0

# --- AWS / Bedrock ---
boto3>=1.35.0

# --- read-only DB access (Neon pooler compatible) ---
psycopg[binary]==3.3.5

# --- link checking (L1 HTTP + redirect chains) ---
httpx>=0.27.0

# --- L2 stealth fetch (only where L1 is inconclusive) ---
# scrapling>=0.2 ; installed on demand, kept optional to avoid heavy deps in CI

# --- structured output / validation ---
pydantic>=2.7.0

# --- surfacing / notifications ---
resend>=2.0.0

# --- evals / telemetry ---
opentelemetry-sdk>=1.25.0

# --- dev / test ---
pytest>=8.2.0
